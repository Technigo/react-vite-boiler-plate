export const Boolean = ({ value }) => {
  return (
    <div>
      {value
        ? "True value thus we print this string"
        : "False value thus we print this opposite string"}
    </div>
  );
};
